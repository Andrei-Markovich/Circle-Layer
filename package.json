{
  "name": "circle-layer",
  "private": true,
  "version": "1.0.0",
  "description": "SimpleStorage on Circle Layer Testnet (Hardhat)",
  "author": "",
  "license": "MIT",
  "engines": {
    "node": ">=18.17"
  },
  "scripts": {
    "clean": "hardhat clean",
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy": "hardhat run --network circleLayerTestnet scripts/deploy.js",
    "read": "hardhat run --network circleLayerTestnet scripts/read.js",
    "export:abi": "node scripts/exportAbi.js"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "hardhat": "^2.22.0",
    "@nomicfoundation/solidity-analyzer-linux-x64-gnu": "^0.1.2"
  }
}
